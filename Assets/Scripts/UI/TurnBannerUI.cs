using UnityEngine;

//Right now TurnBannerController handles the animation as well as the direct UI behavior
// In a later version, I thought we could wrap and expose public UI properties so that other 
// systems can interact with the banner while still having a clean interface. 

public class TurnBannerUI
{
    
}
